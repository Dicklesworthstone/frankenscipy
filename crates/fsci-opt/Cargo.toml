[package]
name = "fsci-opt"
version = "0.1.0"
edition = "2024"

[features]
opt-incumbent-bench = ["dep:sha2"]

[dependencies]
fsci-runtime.workspace = true
rand.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2 = { version = "0.10.9", optional = true }

[dev-dependencies]
criterion = { workspace = true }
proptest.workspace = true
sha2 = "0.10.9"

[[bench]]
name = "optimize_bench"
harness = false
